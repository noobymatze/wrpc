// Hallo Welt!
#(check "Hello World")
data Greet {
    // Test
    // Multiline Test
    #(check (min 1))
    name: String,
    // Multiline Test
    test: Boolean,
}

// This is an error, that will happen
enum Error {
    // More stuff!
    Hello { name: String, },
    Foo { name: String, },
}

#(route "/foo")
service Foo {

    #(get "/test")
    def foo(name: String): Greet

    def test(name: String): Result<Error, Greet>

}
