// Hallo Welt!
#(check "Hello World")
data Greet {
    // Test
    // Multiline Test
    #(check (>= (len .name) 1))
    name: String,
    // Multiline Test
    test: Boolean,
}

enum State {
    Waiting,
    Done,
}

// This is an error, that will happen
enum Error {
    // More stuff!
    Hello { name: String, foo: Int32 },
    Foo { name: String, },
}

// And another comment for foo
#(route "/foo")
service FooService {

    // This is a comprehension doc comment for the foo
    // method. It is brilliant because.
    #(get "/test" {:test "hoho"})
    def foo(name: String): Greet

    def test(name: String): Result<Error, Greet>

}
